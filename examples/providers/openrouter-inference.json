{
  "_comment1": "OpenRouter inference-cost prototype. Tracks per-model spend using",
  "_comment2": "the dynamic pricing-lookup feature (slice 2):",
  "_comment3": "",
  "_comment4": "  1. On startup, tray fetches /api/v1/models and caches the",
  "_comment5": "     per-model price table.",
  "_comment6": "  2. The sidecar probes OpenRouter with a real chat completion",
  "_comment7": "     every N seconds, captures usage + model id, and emits",
  "_comment8": "     parser-shaped JSON with `model` field populated.",
  "_comment9": "  3. The burn row appends '· $X/h' using",
  "_comment10": "    `pricing::cost_per_hour()` for any model the table knows.",
  "_comment11": "",
  "_comment12": "DIFFERENCE FROM openrouter-prototype.json (slice 1):",
  "_comment13": "  - slice 1: dollar balance via /api/v1/auth/key, uses",
  "_comment14": "    count_unit: \"cents\" so the row reads '$0.4/h' directly.",
  "_comment15": "  - this file: token usage via inference probe, uses",
  "_comment16": "    pricing_endpoint + pricing_model_path so the row reads",
  "_comment17": "    '40 tok/h · $0.4/h' (both rates visible).",
  "_comment18": "",
  "_comment19": "WHEN TO USE WHICH:",
  "_comment20": "  - Prefer slice 1 if OpenRouter's auth/key endpoint is",
  "_comment21": "    accessible with your API key (gives total dollar spend).",
  "_comment22": "  - Prefer this slice if you want per-model cost attribution",
  "_comment23": "    (each model gets its own row with its own $/h).",
  "_comment24": "",
  "_comment25": "Run with:",
  "_comment26": "  llm-quota-tray --instance=openrouter-inference",

  "endpoint": "http://127.0.0.1:8088/openrouter/inference",
  "dashboard_url": "https://openrouter.ai/credits",
  "label": "OpenRouter (per-model)",

  "shape": {
    "entries_path": "/",
    "windows": [{
      "id": "session",
      "field_prefix": "session",
      "start_unit_ms": 1000,
      "reset_unit_ms": 1,
      "reset_is_absolute_epoch": false,
      "pricing_model_path": "/model",
      "_win_comment1": "Adapter reads from OpenRouter's chat completion response,",
      "_win_comment2": "emits a single entry per sidecar poll. The `model` field",
      "_win_comment3": "is the model id (e.g. \"openai/gpt-4o\") used for the",
      "_win_comment4": "pricing lookup. session_remaining_percent is adapter-set",
      "_win_comment5": "since OpenRouter doesn't return a quota cap."
    }],
    "error_envelope": null
  },

  "ring_colors": {
    "inner": {
      "_inner_comment": "Same purple as slice 1; different label distinguishes.",
      "normal":   "#7c3aed",
      "warning":  "#fbbf24",
      "throttled": "#dc2626"
    },
    "outer": "#4f46e5"
  },

  "auth": {
    "type": "bearer",
    "_auth_comment": "Bearer auth is used by the SIDE PROXY when it makes the upstream",
    "_auth_comment2": "request to OpenRouter. The tray itself doesn't reach OpenRouter",
    "_auth_comment3": "directly -- it reads the proxy's adapter-shaped JSON."
  },

  "user_agent": "llm-quota-tray",

  "refresh_seconds": 30,
  "refresh_min_seconds": 15,
  "refresh_max_backoff_seconds": 600,
  "_refresh_comment": "30s -- inference probe is cheap but adds latency; 30s balances freshness",

  "thresholds": {
    "yellow": 60,
    "red": 85
  },

  "burn_warning": {
    "enabled": true,
    "min_history_ms": 300000,
    "lookback_ms": 1800000,
    "use_epoch_average": true,
    "_burn_comment": "5-min min history (vs the default 10-min) -- 30s polls",
    "_burn_comment2": "accumulate samples fast enough that the burn-rate slope",
    "_burn_comment3": "is meaningful after 5 minutes. Lookback at 30min -- beyond",
    "_burn_comment4": "that, OpenRouter's per-model pricing can change with model",
    "_burn_comment5": "deprecations, so older samples aren't representative."
  },

  "pricing_endpoint": "https://openrouter.ai/api/v1/models",
  "pricing_refresh_polls": null,
  "_pricing_comment": "OpenRouter's /api/v1/models is public (no auth needed).",
  "_pricing_comment2": "pricing_refresh_polls: null means fetch once at startup, never refresh.",
  "_pricing_comment3": "Set to e.g. 360 to re-fetch every 360 successful polls (3 hours at the default 30s cadence).",

  "_adapter_example": {
    "_note": "Required: a small sidecar that probes OpenRouter every N seconds",
    "_note2": "and emits the parser-shaped JSON. Runs on 127.0.0.1:8088.",
    "_note3": "Replace sk-or-v1-... with your actual OpenRouter API key.",

    "_python_stub": "#!/usr/bin/env python3",
    "_python_stub2": "\"\"\"Minimal OpenRouter inference-cost proxy.",
    "_python_stub3": "Probes /api/v1/chat/completions every N seconds, captures",
    "_python_stub4": "usage + model id, and emits the tray's parser-shaped JSON.",
    "_python_stub5": "\"\"\"",
    "_python_stub6": "",
    "_python_stub7": "import http.server, os, urllib.request, json, time",
    "_python_stub8": "",
    "_python_stub9": "OR_KEY        = os.environ['OPENROUTER_API_KEY']",
    "_python_stub10": "OR_MODEL      = os.environ.get('OPENROUTER_MODEL', 'deepseek/deepseek-v4-flash')",
    "_python_stub11": "POLL_EVERY    = int(os.environ.get('OPENROUTER_POLL_EVERY', '25'))  # seconds",
    "_python_stub12": "DAILY_CAP_TOK = int(os.environ.get('OPENROUTER_DAILY_CAP', '1000000'))  # tier's daily cap",
    "_python_stub13": "",
    "_python_stub14": "state = {'last_poll': 0, 'cached': None, 'fail_streak': 0}",
    "_python_stub15": "",
    "_python_stub16": "def probe():",
    "_python_stub17": "    \"\"\"Send a 1-token chat probe and capture usage + model id.\"\"\"",
    "_python_stub18": "    body = json.dumps({",
    "_python_stub19": "        'model': OR_MODEL,",
    "_python_stub20": "        'messages': [{'role': 'user', 'content': 'hi'}],",
    "_python_stub21": "        'max_tokens': 1,",
    "_python_stub22": "    }).encode()",
    "_python_stub23": "    req = urllib.request.Request(",
    "_python_stub24": "        'https://openrouter.ai/api/v1/chat/completions',",
    "_python_stub25": "        data=body,",
    "_python_stub26": "        headers={'Authorization': f'Bearer {OR_KEY}',",
    "_python_stub27": "                   'Content-Type': 'application/json'})",
    "_python_stub28": "    with urllib.request.urlopen(req) as r:",
    "_python_stub29": "        resp = json.load(r)",
    "_python_stub30": "    u = resp.get('usage', {})",
    "_python_stub31": "    prompt_tokens = int(u.get('prompt_tokens', 0))",
    "_python_stub32": "    completion_tokens = int(u.get('completion_tokens', 0))",
    "_python_stub33": "    total_used = prompt_tokens + completion_tokens",
    "_python_stub34": "",
    "_python_stub35": "    # Adapter computes remaining_pct against the user's tier cap.",
    "_python_stub36": "    # OpenRouter doesn't return this; the tray needs *some* value",
    "_python_stub37": "    # to render the bar. We track cumulative daily usage across",
    "_python_stub38": "    # processes (in /tmp/or-counter) for a sliding 24h total.",
    "_python_stub39": "    counter_path = '/tmp/or-counter-' + OR_MODEL.replace('/', '_')",
    "_python_stub40": "    try:",
    "_python_stub41": "        with open(counter_path) as f:",
    "_python_stub42": "            cum = int(f.read().strip() or '0') + total_used",
    "_python_stub43": "    except (FileNotFoundError, ValueError):",
    "_python_stub44": "        cum = total_used",
    "_python_stub45": "    with open(counter_path, 'w') as f:",
    "_python_stub46": "        f.write(str(cum))",
    "_python_stub47": "    remaining_pct = max(0, min(100, int(100 * (1 - cum / DAILY_CAP_TOK))))",
    "_python_stub48": "",
    "_python_stub49": "    now_ms = int(time.time() * 1000)",
    "_python_stub50": "    return {",
    "_python_stub51": "        'model': OR_MODEL,",
    "_python_stub52": "        'session_total_count': DAILY_CAP_TOK,",
    "_python_stub53": "        'session_usage_count': total_used,",
    "_python_stub54": "        'session_remaining_percent': remaining_pct,",
    "_python_stub55": "        'start_time': now_ms // 1000,",
    "_python_stub56": "        'remains_time': 86_400_000,  # 24h placeholder; reset_is_absolute_epoch=false",
    "_python_stub57": "    }",
    "_python_stub58": "",
    "_python_stub59": "class Q(http.server.BaseHTTPRequestHandler):",
    "_python_stub60": "    def do_GET(self):",
    "_python_stub61": "        now = time.time()",
    "_python_stub62": "        if state['cached'] is None or (now - state['last_poll']) >= POLL_EVERY:",
    "_python_stub63": "            try:",
    "_python_stub64": "                state['cached'] = probe()",
    "_python_stub65": "                state['last_poll'] = now",
    "_python_stub66": "                state['fail_streak'] = 0",
    "_python_stub67": "            except Exception as e:",
    "_python_stub68": "                state['fail_streak'] += 1",
    "_python_stub69": "                # If we've never had a successful probe, return",
    "_python_stub70": "                # a synthetic empty payload so the tray renders",
    "_python_stub71": "                # its 'no data yet' state instead of hammering",
    "_python_stub72": "                # us with retries on every poll.",
    "_python_stub73": "                if state['cached'] is None:",
    "_python_stub74": "                    body = json.dumps({",
    "_python_stub75": "                        'session_total_count': 0,",
    "_python_stub76": "                        'session_usage_count': 0,",
    "_python_stub77": "                        'session_remaining_percent': 100,",
    "_python_stub78": "                        'start_time': now,",
    "_python_stub79": "                        'remains_time': 86_400_000,",
    "_python_stub80": "                    }).encode()",
    "_python_stub81": "                    self.send_response(200)",
    "_python_stub82": "                    self.send_header('content-type', 'application/json')",
    "_python_stub83": "                    self.send_header('content-length', str(len(body)))",
    "_python_stub84": "                    self.end_headers()",
    "_python_stub85": "                    self.wfile.write(body)",
    "_python_stub86": "                    return",
    "_python_stub87": "        body = json.dumps(state['cached']).encode()",
    "_python_stub88": "        self.send_response(200)",
    "_python_stub89": "        self.send_header('content-type', 'application/json')",
    "_python_stub90": "        self.send_header('content-length', str(len(body)))",
    "_python_stub91": "        self.end_headers()",
    "_python_stub92": "        self.wfile.write(body)",
    "_python_stub93": "    def log_message(self, *a): pass",
    "_python_stub94": "",
    "_python_stub95": "http.server.HTTPServer(('127.0.0.1', 8088), Q).serve_forever()",

    "_note3": "",
    "_note4": "Run with:",
    "_note5": "  OPENROUTER_API_KEY=sk-or-v1-... \\",
    "_note6": "    OPENROUTER_MODEL=openai/gpt-4o \\",
    "_note7": "    OPENROUTER_DAILY_CAP=1000000 \\",
    "_note8": "    python3 proxy.py",
    "_note9": "",
    "_note10": "WHAT YOU'LL SEE:",
    "_note11": "  - Chip: 'OpenRouter (per-model) 95%' (or wherever the daily cap is)",
    "_note12": "  - Burn row: '· on pace to have ~95% left at reset (40 tok/h · $0.4/h)'",
    "_note13": "    The first rate is tokens/h (from the parser); the second is",
    "_note14": "    dollars/h (computed from the cached price table for the model",
    "_note15": "    named in the adapter's response).",
    "_note16": "  - On model deprecation or pricing change, the cost fragment",
    "_note17": "    updates on the next tray restart (or on the next pricing",
    "_note18": "    refresh interval if you set pricing_refresh_polls).",

    "_note17": "CAVEATS:",
    "_note18": "  - The adapter's cumulative counter assumes a 24h reset at startup",
    "_note19": "    time. For accurate per-day tracking, set the proxy to start at",
    "_note20": "    midnight UTC, or persist the counter + last-reset timestamp and",
    "_note21": "    reset on day rollover.",
    "_note22": "  - The probe sends a real inference request (1 token out). That's",
    "_note23": "    a few cents/day of overhead at most model price points; not free",
    "_note24": "    but negligible.",
    "_note25": "  - prompt_share is hardcoded to 0.5 in main.rs (50/50 prompt vs",
    "_note26": "    completion split). If your workload skews heavily to one side,",
    "_note27": "    the cost fragment is a rough estimate. The full split would",
    "_note28": "    need the parser to expose prompt_tokens and completion_tokens",
    "_note29": "    separately -- out of scope for this slice."
  }
}